# Production dependencies for Lambda - LangChain v1.2.6 compatible
# Core AWS SDK
boto3>=1.35.0

# LangChain stack (lean, no meta-package)
langchain-core>=0.3.0
langchain-aws>=0.2.0
langgraph>=0.2.0

# Utility dependencies
python-dateutil>=2.8.2
requests>=2.31.0

# Pydantic for data validation (required by LangChain)
pydantic>=2.5.0,<3.0.0
